interface:
  display_name: "WeChat Analysis Write Publisher"
  short_description: "Research, review, and prepare WeChat account drafts"
  default_prompt: "Use $wechat-analysis-write-publisher to analyze a topic and prepare a reviewed WeChat draft."

policy:
  allow_implicit_invocation: true
